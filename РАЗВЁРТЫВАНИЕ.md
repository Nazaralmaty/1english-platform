# Путь до production ready

Оценки на 11.09.2026. Цены проверить перед оплатой.

## Где мы сейчас

| Что | Состояние |
|---|---|
| Платформа открывается по ссылке | ✅ https://nazaralmaty.github.io/1english-platform/ |
| Каждый `git push` выкатывает новую версию | ✅ GitHub Pages из ветки `main` |
| Вход по номеру телефона | ✅ Supabase Auth, аккаунт заводится сам при первом входе |
| Профиль и прогресс уезжают в базу | ⏳ **нужен один шаг, 2 минуты** — см. ниже |
| Домен `app.1eng.kz` | ⏳ по желанию, 1 день |
| Дашборд по всем ученикам | ✅ `/dashboard.html`, обновляется раз в 10 секунд |
| Согласие на обработку данных | ✅ галочка при входе, дата пишется в базу |
| Ответы не видно в исходнике | ❌ 2 дня работы |
| Защита кода входа на сервере | ❌ полдня |

Тильда на `1eng.kz` не тронута и трогать её не нужно.

## Шаг 1. Включить базу — 2 минуты, делаете вы

Без этого вход работает, а данные ложатся только на телефон ученика.

1. Открыть [supabase.com/dashboard](https://supabase.com/dashboard), проект
   `ckayydaqncvnbecjlltf` — тот же, где живут игры.
2. Слева **SQL Editor** → **New query**.
3. Открыть файл [`backend/supabase_min.sql`](backend/supabase_min.sql),
   скопировать целиком, вставить в редактор.
4. Нажать **Run**.

Внизу должно появиться две строки: `en_students` и `en_progress`, у обеих
`rls_on = true`. Всё, база включена. Скрипт можно запускать повторно, он
ничего не ломает.

## Шаг 2. Проверить, что данные пишутся — 1 минута

1. Открыть платформу, войти по своему номеру, придумать код из шести цифр.
2. Выбрать уровень, открыть любой урок, нажать «Понятно».
3. В Supabase: **Table Editor** → `en_students` — там появилась строка с
   вашим номером. В `en_progress` — строка с уроком и шагом `read`.

Если строк нет — открыть платформу, нажать F12 → Console и посмотреть
строки `[db]`: там написана причина словами.

**Проверка, ради которой всё это делалось:** войти с тем же номером и
кодом с другого телефона. Прогресс и профиль должны приехать за вами.

## Шаг 3. Открыть дашборд

Дашборд лежит по адресу
`https://nazaralmaty.github.io/1english-platform/dashboard.html` и
показывает всех учеников: уровень, процент, сколько уроков закрыто, когда
заходил последний раз. Обновляется сам раз в десять секунд, вкладка в
фоне не опрашивает базу.

Чтобы он показал не только вас, аккаунт должен лежать в таблице
`en_admins`. В SQL Editor, подставив свой номер:

```sql
insert into public.en_admins (id, note)
select id, 'Элжан' from auth.users where email = '77011234567@1eng.kz'
on conflict (id) do nothing;
```

Пока аккаунта там нет, дашборд честно скажет об этом и не покажет чужие
строки: так работают правила RLS, а не проверка в браузере.

## Шаг 4. Свой домен — 1 день, по желанию

`1eng.kz` остаётся на Тильде. Платформа живёт на поддомене
**`app.1eng.kz`**: у регистратора добавляется одна CNAME-запись на
`nazaralmaty.github.io`, в настройках репозитория GitHub Pages
прописывается Custom domain. Тильда об этом не знает. Запись удаляется за
минуту, если поддомен не подойдёт.

## Что осталось до production ready

**Ф1. Ответы на сервере — 2 дня.**
Сейчас `app/course.js` уходит в браузер целиком, вместе с индексами верных
ответов. Пока учеников десяток и это свои — не страшно. Как только доступ
получит чужой ребёнок: вопросы отдаёт функция Postgres, ответ проверяется
на сервере, в браузер приходит «верно / неверно» по одному заданию.
Скачать банк целиком станет невозможно.

**Ф2. Удаление аккаунта — полдня.**
Согласие и его дата пишутся при входе, текст короткий и лежит на экране
входа. Осталась вторая половина закона: кнопка «удалить мои данные» и
удаление по запросу. Сейчас это делается руками в панели Supabase.

**Ф3. Нагрузка, бэкапы, мониторинг — 1 день.**
k6: 300 виртуальных учеников, сценарий «вход, урок, шесть заданий, восемь
слов», смотреть 95-й перцентиль. Supabase Pro делает ежедневные бэкапы
сам, к ним — выгрузка раз в неделю в отдельное место. Ошибки фронта —
Sentry на бесплатном тарифе.

**Ф4. Код входа — полдня.**
Сейчас после пяти промахов ввод замирает на минуту, но это защита от
соседа с чужим телефоном: скрипт пойдёт мимо экрана, прямо в API.
Настоящая защита — список разрешённых номеров: преподаватель заводит
номер, платформа пускает только его. Плюс сброс кода, если ученик забыл:
сейчас это делается удалением пользователя в панели Supabase.

Итого после шага 1: **4 рабочих дня** до состояния, когда платформу можно
давать незнакомому ученику.

## Чек-лист production ready

- [ ] RLS включён на каждой таблице, политики проверены от лица ученика,
      родителя и преподавателя. Без этого чужой прогресс читается любым
      залогиненным.
- [ ] Индекс верного ответа не уходит в браузер.
- [ ] Прогресс переезжает между телефонами и переживает переустановку
      браузера.
- [ ] Ежедневные бэкапы и **проверенное** восстановление.
- [x] Согласие на обработку персональных данных с датой.
- [ ] Кнопка «удалить мои данные».
- [ ] Мониторинг ошибок фронта и алерт на падение базы.
- [ ] Нагрузочный прогон на 300 учеников пройден.
- [ ] Код входа защищён от подбора на сервере, а не только в браузере.
- [ ] Сброс кода, если ученик его забыл.

## Что открыто в пробном варианте и почему это нормально

**anon-ключ виден в исходнике — так и задумано.** Он не даёт доступа сам
по себе: правила RLS разрешают ученику читать и писать только свои
строки. Опасен другой ключ, `service_role` — его в браузере нет и быть не
должно. Ключ, который нельзя показывать, в этом проекте один, и он лежит
только в панели Supabase.

**HTML и содержание уроков скачиваются целиком.** Тексты правил и слова —
не секрет. Секрет только индексы верных ответов, и это чинится в Ф1.

**Чужой человек может завести аккаунт.** Пока вход открыт всем, кто знает
ссылку. Закрывается списком разрешённых номеров: преподаватель заводит
номер, платформа пускает только его.

## Деньги

| Что | Чем закрываем | В месяц |
|---|---|---|
| Статика | GitHub Pages | 0 ₸ |
| Видеоуроки, 56 роликов | YouTube unlisted | 0 ₸ |
| База и вход | Supabase Free до ~50 учеников | 0 ₸ |
| То же, от 50 учеников | Supabase Pro | ~12 500 ₸ ($25) |
| Поддомен `app.1eng.kz` | у регистратора | 0 ₸ |
| SMS-коды | не подключаем | 0 ₸ |

Supabase Free засыпает после недели без запросов, и первый ученик ждёт
секунд тридцать. Пока платформа живая каждый день — это не мешает.

## Почему хватает мощности

Один ученик за занятие делает 30–60 запросов. 300 учеников в один вечер —
около 15 000 запросов, пик 20–30 в секунду, если все сели в 19:00.
Supabase Pro держит на порядок больше. Тяжёлое в платформе только видео,
а оно лежит на YouTube и раздаётся его CDN бесплатно.
